const api = window.PluginApi;

const { React, libraries, hooks, GQL, patch, utils, components } = api;
const Apollo = libraries.Apollo;
const gql = Apollo.gql;

export { React, libraries, hooks, GQL, patch, utils, Apollo, gql, components };
